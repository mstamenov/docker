Image with windows server 2022 core and iis with .NET Framework 4.5 runtime and installed IIS components.

Build an image:
```bash
docker build https://github.com/mstamenov/docker.git#:windows/srv-frm48 -t win-frm48:v1
```